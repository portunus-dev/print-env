dist: xenial
language: python
python:
  - "3.6"
  - "3.7"
  - "3.8"
# command to install dependencies
before_install:
  - pip install -U pip pipenv
install:
  - pipenv install --dev
# command to run tests
script:
  - flake8 print_env/
  - pytest
